import BarcodeMaster from '@/components/Barcode/BarcodeMaster';
import React from 'react';

const index = () => {
  return (
    <div>
      <BarcodeMaster />
    </div>
  );
};

export default index;
